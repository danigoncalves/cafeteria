git config --global user.name "Dani"
git config --global user.name "daniigoncalves99@gmail.com"


SEMPRE COM A PASTA ABERTA NO TERMINAL DO GIT


Pra enviar pro git

git add .
git commit -m "Mensagem sobre a alteração"
git push -u origin master


Pra baixar do git:

Pela primeira vez:

git clone https://github.com/danigoncalves/cafeteria.git

Outras vezes:
git pull